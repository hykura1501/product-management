mixin product-category-tree(items, level = 1)
	each item in items
		-var prefix = "-- ".repeat(level)
		option(value=item.id selected=item.isTrue)=prefix + item.title 
		if (item.children && item.children.length > 0)
			+product-category-tree(item.children, level + 1)
