extends ../../layouts/default.pug
include ../../mixins/box-head.pug
include ../../mixins/product-layout.pug
block main 
  .container.my-5
    .row.d-flex.justify-content-center
      .col-6
        div.text-center
          +box-head(pageTitle)
        form(action="/user/password/otp" method="POST" class="d-flex flex-column")
          div
            div(class="form-group mt-3 mb-3") 
              label(for="email" class="form-label") Email
              input(type="text" name="email" id="email" class="form-control" value=email readonly) 
            div(class="form-group mt-3 mb-3") 
              label(for="otp" class="form-label") OTP
              input(type="text" name="otp" id="otp" class="form-control") 
          div.d-flex.justify-content-center
            button(type="submit" class="btn btn-success") Lấy lại mật khẩu

